apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.serviceC.name }}
  labels:
    app: {{ .Values.serviceC.name }}
spec:
  replicas: {{ .Values.serviceC.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.serviceC.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.serviceC.name }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.serviceC.containerPort }}"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: {{ .Values.serviceC.name }}
          image: {{ .Values.serviceC.image }}
          ports:
            - containerPort: {{ .Values.serviceC.containerPort }}
              name: http
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: nginx-config
          configMap:
            name: {{ .Values.serviceC.name }}-config